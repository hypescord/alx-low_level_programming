#include <stdio.h>
#include <string.h>
/**
 *main - Entry point
 *Return: always 0 (success)
 */

int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);

	return (0);
}
