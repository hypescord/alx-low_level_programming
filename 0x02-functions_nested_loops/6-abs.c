#include "main.h"

/**
 * _abs - compute absolute value of integer
 * @n: character to be tested
 * Return: integer
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
