#include <stdio.h>
#include <stdlib.h>

/**
 *main - Entry point
 *Return: success
 */

int main(void)
{
	int num = 0;

	while (num < 10)
	{
		printf(num);
		num++;
	}
	printf('\n');
	
	return (0);
}
