#include <stdio.h>

/**
 *main - Entry point
 *Return: success
 */

int main(void)
{
	int digit = '0';

	while (digit <= '9')
	{
		if (digit != '9')
		{
			putchar(digit);
			putchar(',');
			putchar(' ');
		}
		digit++;
	}
	putchar('\n');
	return (0);
}
