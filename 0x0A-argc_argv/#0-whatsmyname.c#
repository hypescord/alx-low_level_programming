#include <stdio.h>

/**
 *main - print file name
 *@argc: counter
 *@argv: elements
 *Return: 0 success
 */

int main(int argc, char *argv[])
{
	if (argc > 1)
		printf("%s\n", argv[argc - 3]);

	return (0);
}
